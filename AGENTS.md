# AGENTS.md

本仓库：go-csust-planet（Go 后端，含 `admin` Svelte 管理后台）。

## 修改前需求确认

- 对本仓库中的任何文件进行修改前，必须读取并遵循 `grilling` skill。
- 执行 `grilling` skill 时必须使用提问工具；如果提问工具不可用，则改用普通文本。
- 在 `grilling` skill 要求的最终确认环节，必须先完整汇总共识，再单独询问用户是否确认。
- 仅当用户在当次请求中明确要求“立即执行并忽略 grilling”时，才可以跳过上述流程。该豁免仅对当次请求有效。

## 编译验证与测试

- 修改后端或管理后台代码、测试、构建配置、依赖或 CI 相关文件后，必须读取并遵循 `verify-after-change` skill。
- 根据本次修改的影响范围，严格按该 skill 规定的顺序执行编译/检查与测试，不自行替换验证命令。

## 代码格式化

- 修改 Go 代码后，对涉及的文件执行 `gofmt`。
- 修改管理后台代码后，在 `admin` 目录执行 `pnpm format`。
